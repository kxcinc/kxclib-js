# kxclib-version-info

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build kxclib-version-info` to build the library.

## Running unit tests

Run `nx test kxclib-version-info` to execute the unit tests via [Jest](https://jestjs.io).
